# Repository Instructions For GitHub Copilot

Follow the shared repository instructions in
`.github/instructions/repository.instructions.md`.

Also follow additional `.github/instructions/*.instructions.md` files when
their `applyTo` pattern matches or the shared repository instructions route
the task to them by subject.

Read the relevant `.github/agents/` entry point when taking a specialist role.
Mandatory domain instructions apply regardless of role.
