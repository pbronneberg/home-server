# Legacy Infrastructure References

Files in this directory are rollback references from before the Flux layout.
They are not reconciled by the cluster.

Use `clusters/home/infrastructure/` for active GitOps infrastructure state.
