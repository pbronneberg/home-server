apiVersion: v1
kind: Service
metadata:
  name: {{ include "photobooth-app.fullname" . }}
  labels:
    {{- include "photobooth-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "photobooth-app.selectorLabels" . | nindent 4 }}
