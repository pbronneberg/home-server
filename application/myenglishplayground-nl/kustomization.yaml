apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: websites
resources:
  - helmrelease.yaml
configMapGenerator:
  - name: myenglishplayground-nl-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
