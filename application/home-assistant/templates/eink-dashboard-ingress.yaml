{{- if .Values.einkDashboardIngress.enabled }}
{{- $host := (index .Values.ingress.hosts 0).host }}
{{- $tlsSecret := (index .Values.ingress.tls 0).secretName }}
{{- $serviceName := .Values.resourceNames.service | default (include "home-assistant.fullname" .) }}
{{- $publicImagePath := required "einkDashboardIngress.publicImagePath must be set" .Values.einkDashboardIngress.publicImagePath }}
{{- $middlewares := deepCopy .Values.einkDashboardIngress.middlewares }}
{{- if .Values.forwardedHeaders.stripForHomeAssistant.enabled }}
{{- $stripMiddleware := printf "%s-%s@kubernetescrd" .Release.Namespace (printf "%s-strip-forwarded-headers" (include "home-assistant.fullname" .)) }}
{{- $middlewares = append $middlewares $stripMiddleware }}
{{- end }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "home-assistant.fullname" . }}-eink-dashboard
  labels:
    {{- include "home-assistant.labels" . | nindent 4 }}
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    traefik.ingress.kubernetes.io/router.middlewares: {{ join "," $middlewares | quote }}
    traefik.ingress.kubernetes.io/router.priority: {{ .Values.einkDashboardIngress.priority | quote }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  tls:
    - hosts:
        - {{ $host | quote }}
      secretName: {{ $tlsSecret }}
  rules:
    - host: {{ $host | quote }}
      http:
        paths:
          - path: {{ $publicImagePath | quote }}
            pathType: Exact
            backend:
              service:
                name: {{ $serviceName }}
                port:
                  number: {{ .Values.service.port }}
{{- end }}
