apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bronneberg-org.fullname" . }}-test-connection"
  labels:
    {{- include "bronneberg-org.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bronneberg-org.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
