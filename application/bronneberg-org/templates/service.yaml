apiVersion: v1
kind: Service
metadata:
  name: {{ include "bronneberg-org.fullname" . }}
  labels:
    {{- include "bronneberg-org.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bronneberg-org.selectorLabels" . | nindent 4 }}
