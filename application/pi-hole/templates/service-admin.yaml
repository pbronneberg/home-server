apiVersion: v1
kind: Service
metadata:
  name: {{ include "pi-hole.fullname" . }}-admin
  labels:
    {{- include "pi-hole.labels" . | nindent 4 }}
spec:
  selector:
    app: pihole
  ports:
  - port: 80
    targetPort: 80
    name: pihole-admin
  type: ClusterIP