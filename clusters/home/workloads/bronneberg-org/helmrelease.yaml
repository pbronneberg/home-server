apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: bronneberg-org
  namespace: websites
spec:
  chart:
    spec:
      chart: ./application/bronneberg-org
      interval: 1h
      reconcileStrategy: Revision
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  install:
    remediation:
      retries: 3
  interval: 30m
  maxHistory: 5
  releaseName: bronneberg-org
  timeout: 5m
  upgrade:
    remediation:
      retries: 3
