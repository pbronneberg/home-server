apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helmrelease.yaml
  - shutdown-rbac.yaml
  - worker-group.yaml
  - milliard-node.yaml
  # marvin-node.yaml is kept inactive until USB-network Wake-on-LAN is fixed.
