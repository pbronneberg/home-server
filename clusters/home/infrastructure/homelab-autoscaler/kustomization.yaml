apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helmrelease.yaml
  - shutdown-rbac.yaml
  - worker-group.yaml
  - marvin-node.yaml
  # milliard-node.yaml is kept inactive until integrated Ethernet WOL is fixed.
