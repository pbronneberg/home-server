apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helmrelease.yaml
  - shutdown-rbac.yaml
  - wol-relay.yaml
  - worker-group.yaml
  # homecluster-dev/homelab-autoscaler validates Node CRs against existing
  # Kubernetes Nodes. Enable marvin-node.yaml after marvin has rejoined.
  - milliard-node.yaml
