apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: kairos-kubevirt-staging
    home-server.dev/evaluation: kairos
  name: kairos-k3s-api
  namespace: vms
spec:
  ports:
    - name: k3s-api
      port: 6443
      protocol: TCP
      targetPort: 6443
  selector:
    vm.kubevirt.io/name: kairos-server
  type: ClusterIP
